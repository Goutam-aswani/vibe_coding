version: '3.8'

services:
  api:
    build: .
    container_name: email-verifier-api
    ports:
      - "8000:8000"
    environment:
      - EMAILVERIFIER_SESSION_COOKIE=${EMAILVERIFIER_SESSION_COOKIE}
      - API_KEY=${API_KEY:-}
      - HOST=0.0.0.0
      - PORT=8000
      - WORKERS=4
      - RATE_LIMIT_DELAY=1.0
      - MAX_CONCURRENT=5
      - LOG_LEVEL=INFO
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; httpx.get('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Optional: Redis for caching and job persistence
  # redis:
  #   image: redis:7-alpine
  #   container_name: email-verifier-redis
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis-data:/data
  #   restart: unless-stopped

# volumes:
#   redis-data:
