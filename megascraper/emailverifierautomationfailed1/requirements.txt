# Requirements for Email Checker Automation

# Core dependencies (both methods)
httpx>=0.27.0

# For HTTP compression support (REQUIRED for check.emailverifier.online)
brotli>=1.1.0
zstandard>=0.25.0

# For Playwright method (free)
playwright>=1.40.0

# For 2Captcha method (paid)
twocaptcha-python>=1.2.0

# For API server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic[email]>=2.5.0

# Optional: for DNS checking
dnspython>=2.4.0

# Optional: for better async support
aiofiles>=23.2.0
